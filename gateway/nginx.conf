# gitea.localhost
server {
    listen 8080;
    server_name gitea.localhost;

    location / {
        proxy_pass http://gitea:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# audit.localhost
server {
    listen 8080;
    server_name audit.localhost;

    location / {
        proxy_pass http://audit:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# mark.localhost
server {
    listen 8080;
    server_name mark.localhost;

    location / {
        proxy_pass http://mark:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Anthropic API proxy — whitelisted upstream for sandboxed agents
server {
    listen 8443;
    server_name _;

    location / {
        proxy_pass https://api.anthropic.com;
        proxy_ssl_server_name on;
        proxy_set_header Host api.anthropic.com;

        # Support streaming (SSE)
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }
}

